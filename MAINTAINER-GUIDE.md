# คู่มือคนดูแล

ใบนี้สำหรับคนที่รับผิดชอบเครื่องมือนี้ ไม่ใช่คู่มืออ่านรวดเดียวจบ แต่เป็นของที่เปิดหาเป็นหัวข้อเวลาต้องใช้

- สิ่งที่ทำเดือนละครั้ง อยู่ใน **`HANDOVER-CARD-OPERATOR.md`** (การ์ดรอบเดือน)
- สิ่งที่คนใช้ข้อมูลต้องรู้ อยู่ใน **`HANDOVER-CARD-READER.md`**
- ใบนี้คือทุกอย่างที่เหลือ

repo นี้รันบน **macOS/Linux** ทุกคำสั่งด้านล่างเป็น bash (ต้นฉบับที่พอร์ตมาเขียนไว้สำหรับ Windows PowerShell) และ X-13 binary ต้องติดตั้งเองครั้งแรกตามหัวข้อ 5

---

## บทบาทนี้รับผิดชอบอะไร

| งาน | ทำบ่อยแค่ไหน | อยู่หัวข้อไหน |
|---|---|---|
| เก็บข้อมูลรอบเดือน | เดือนละครั้ง | การ์ดรอบเดือน |
| เผยแพร่ให้คนอื่นเห็น | หลังรอบเดือนผ่าน | หัวข้อ 2 |
| เพิ่มหรือถอดคำค้น | นานๆ ครั้ง | หัวข้อ 3 |
| ตอบว่าตัวเลขไหนพูดได้ | ทุกครั้งที่มีคนถาม | หัวข้อ 4 |
| ตั้งเครื่องใหม่ | เมื่อเปลี่ยนเครื่องหรือเพิ่มคน | หัวข้อ 5 |
| แก้เมื่อระบบพังจากฝั่ง Google | คาดเดาไม่ได้ | หัวข้อ 6 |

---

## 1. สองทางที่ทำงานได้ เลือกได้ตามถนัด

**ทางที่ 1 ใช้ AI agent** เปิด AI agent (Claude Code, Codex หรือตัวอื่น) ที่โฟลเดอร์นี้ แล้วสั่งเป็นภาษาคนได้เลย
ในโฟลเดอร์มีไฟล์ `CLAUDE.md` และ `AGENTS.md` ที่จะชี้ให้ agent ไปอ่าน `SKILL.md` เอง **ไม่ต้องอธิบายอะไรให้มันฟังก่อน** ใน `SKILL.md` มีกติกาทั้งหมด ขั้นตอนทุกงาน และตารางแก้ปัญหาอยู่ครบแล้ว

สั่งได้ประมาณนี้
- "อัพเดทข้อมูลเดือนนี้"
- "เพิ่มคำว่า ... ให้หน่อย เช็คก่อนว่าเคยลองหรือยัง"
- "ตรวจสุขภาพข้อมูลให้หน่อย"
- "หน้าเว็บ Google เปลี่ยน ตัวเก็บพัง ช่วยดูให้หน่อย"

**ทางที่ 2 พิมพ์คำสั่งเอง** ทุกอย่างสั่งผ่านไฟล์เดียวคือ `scripts/toolkit.sh` คำสั่งอยู่ในแต่ละหัวข้อข้างล่าง

ทางที่ 2 คุมได้แน่นอนกว่าและเหมาะกับงานประจำ ส่วนทางที่ 1 คือทางเดียวที่รอดตอนระบบพังจากฝั่ง Google เพราะงานนั้นต้องแก้โค้ด

> **ไม่ว่าจะใช้ทางไหน ห้ามละเมิดกติกาเหล็กท้ายเล่ม** และถ้าใช้ agent ต้อง**อ่านสิ่งที่มันทำก่อนเสมอ** อย่ากดผ่านรวดเดียว

---

## 2. เผยแพร่ให้คนอื่นเห็น

รอบเดือนจบแล้วข้อมูลยังอยู่แค่ในเครื่องเรา ถ้าอยากให้คนอื่นได้ของใหม่ด้วย ต้องส่งขึ้นต้นฉบับส่วนกลาง

**ทำหลัง `MONTHLY CHECKS PASSED` เท่านั้น** ถ้ายังไม่เขียว ห้ามส่ง

```bash
git status --short
```

ต้องเห็นเฉพาะ 4 อย่างนี้เท่านั้น: `data/series`, `data/catalog.json`, `data.js`, `derived/sa_pipeline_v3`
**ถ้ามีอย่างอื่นโผล่มา ให้หยุดแล้วหาสาเหตุก่อน** อย่าส่งทั้งที่ยังไม่เข้าใจว่าไฟล์นั้นมาจากไหน

```bash
git add -- data/series data/catalog.json data.js derived/sa_pipeline_v3
git diff --cached --name-only
git commit -m "update data through 2026-08"
git push
```

บรรทัดที่สองคือการเปิดดูรายชื่อไฟล์อีกรอบก่อนยืนยัน อย่าข้าม

**ห้ามใช้ `git add -A` เด็ดขาด** เพราะมันจะกวาดไฟล์ชั่วคราวและไฟล์ทดลองที่ไม่ควรขึ้นไปด้วย การระบุชื่อไฟล์ทีละรายการคือสิ่งที่กันไม่ให้ของแปลกปลอมหลุดเข้าไปในต้นฉบับ

ถ้ารอบนั้นมีการแก้รายการคำ ให้เพิ่ม `keywords.csv` เข้าไปในรายการ แต่ต้องตั้งใจเพิ่ม ไม่ใช่เผลอ

---

## 3. เพิ่มหรือถอดคำค้น

### ก่อนอื่น เช็คว่าเคยลองแล้วหรือยัง

`reference/keywords_tried.csv` เก็บคำที่เคยคิดหรือเคยทดสอบไว้ ดูคอลัมน์ `best_stage` ว่าคำนั้นไปตายตรงไหน (repo นี้เพิ่งเริ่ม — ไฟล์นี้ยังว่างเปล่า จะเริ่มสะสมเมื่อทีมคัดกรองคำผ่าน `add_keyword.py --finalize` หรือ `--remove`)

| ถ้าเจอว่า | แปลว่า |
|---|---|
| เคยลองแล้ว NO_DATA | เงียบสนิท อย่าเสียเวลาซ้ำ |
| มี National แต่สัญญาณอ่อน | เคยผ่าน แต่ใช้เป็นสัญญาณไม่ได้ |
| ผ่าน National รอ Regional Gate | ค้างกลางทาง หยิบมาทำต่อได้ |
| ยังไม่พบผล scrape | ยังไม่เคยลองจริง ลองได้ |

### แล้วเก็บข้อมูลคำนั้น แล้วให้ระบบตัดสินให้

ด่านคัดกรองต้องใช้ข้อมูลจริง ตรวจก่อนเก็บไม่ได้ **แต่ต้องตรวจก่อนเอาเข้าคลัง** เพราะถ้าเอาเข้าคลังไปแล้วค่อยพบว่าไม่ผ่าน การถอนออกจะยุ่งมาก (ดูหัวข้อถอนคำ)

**หมายเหตุสำหรับ 18 คำ seed ที่มีตอนสร้าง repo**: คำเหล่านี้ยังไม่ผ่านด่านคัดกรองจริง (ยังไม่มีข้อมูลเก็บ) หลังเก็บรอบแรก ให้รันขั้นตอนนี้ทวนทุกคำแล้วตัด/ปรับ Tier ตามผลจริง ไม่ใช่คงไว้ตามที่ร่าง

ใช้ `monthly-prepare` ไม่ใช่ `monthly-run` เพื่อให้หยุดก่อนขั้นเอาเข้าคลัง

```bash
# 1. สร้างคิวเฉพาะคำนั้น 6 พื้นที่ แล้วเก็บใน Chrome ตามปกติ
./scripts/toolkit.sh monthly-prepare --ids FP004

# 2. รอให้เก็บครบทั้ง 6 พื้นที่ แล้วตรวจจากไฟล์ที่เพิ่งโหลดมา ยังไม่แตะคลัง
.venv/bin/python collector/check_keyword.py FP004 --incoming
```

`--incoming` อ่านไฟล์ใน `incoming/` ตรงๆ **ยังไม่มีอะไรเข้าคลัง** ถ้าคำนั้นไม่ผ่าน แค่ลบแถวใน `keywords.csv` กับลบไฟล์ใน `incoming/` ก็จบ ไม่ต้องตามเก็บกวาดที่ไหนอีก

ถ้าเก็บยังไม่ครบ 6 พื้นที่ มันจะไม่ยอมตัดสินให้ และบอกว่าขาดพื้นที่ไหนบ้าง เพราะดาวน์โหลดที่ยังไม่เสร็จหน้าตาเหมือนคำที่ไม่มีสัญญาณระดับจังหวัดเป๊ะ

**ผ่านแล้วค่อยเอาเข้าคลัง** ด้วย `./scripts/toolkit.sh monthly-finish`

(ถ้าคำนั้นอยู่ในคลังแล้ว ตรวจซ้ำได้ด้วยคำสั่งเดิมโดยไม่ต้องใส่ `--incoming`)

ผลจะออกมาแบบนี้

```
FP004
  ด่าน 1 National : ผ่าน (เดือนศูนย์ 0/150 เดือน เพดาน 37 เดือน ตั้งแต่ 2014-01)
  ด่าน 2 Regional : มีสัญญาณ 5/5 จังหวัด
  ด่าน 3 Tier     : T1 — ผ่านทั้งสองด่าน ใช้เป็นคำเดี่ยวได้
```

### ด่านคัดกรองสามชั้น

| ด่าน | เกณฑ์ | ตกแล้วยังไง |
|---|---|---|
| **1 National** | เดือนที่ค่าเป็นศูนย์ **ไม่เกิน 25%** ของช่วง 2014-01 ถึงปัจจุบัน ที่ระดับประเทศ | คำนั้นบางเกินไป **ไม่ควรรับเข้าชุด** ถอดออกจาก `keywords.csv` |
| **2 Regional** | ต้องมีสัญญาณ **ครบทั้ง 5 จังหวัด** | ยังใช้ได้ แต่เป็นคำเดี่ยวไม่ได้ ต้องเข้าครอบครัว |
| **3 Tier** | สรุปจากสองด่านบน | ดูตารางข้างล่าง |

```
ผ่านด่าน National
      │
      ├── มีสัญญาณ 5/5 จังหวัด ──────→ T1 คำเดี่ยว
      │
      ├── มีสัญญาณ 1-4/5 จังหวัด ────→ T2 ต้องเข้าครอบครัวที่มีอย่างน้อย 2 คำ
      │
      └── ไม่มีสัญญาณจังหวัดเลย ─────→ T3 ใช้เป็น context เท่านั้น ไม่ควรใส่ในชุด
```

**ทำไมด่าน Regional ถึงใช้ "ครบ 5 จังหวัด" ไม่ใช่กฎ 25% เหมือนด่านแรก**
เพราะคำฝั่ง Push (สัญญาณเปราะบาง เช่น *พักหนี้*, *หนี้เสีย*) เป็นเหตุการณ์ที่เกิดไม่บ่อยโดยธรรมชาติ เดือนศูนย์เลยเยอะ ถ้าเอากฎ 25% ไปใช้ที่ระดับภาค **จะกวาดคำฝั่ง Push ออกไปทั้งฝั่ง** แล้วแกน Pull-Push ที่ทั้งดัชนีสร้างอยู่บนนั้นก็พังตาม กฎ 5/5 คือสิ่งที่รักษาฝั่ง Push ไว้

**ข้อควรรู้** `check_keyword.py` เป็นเครื่องช่วยตัดสิน **คำใหม่** เท่านั้น ยังไม่มีคำใดใน repo นี้ผ่านด่านคัดกรองจริงจากข้อมูลเลย (18 คำเริ่มต้นเป็นแค่ seed) การจัดครอบครัวยังมีเหตุผลเชิงความหมายประกอบด้วย ไม่ได้ดูตัวเลขอย่างเดียว

### เพิ่มคำใหม่ผ่าน AI agent (ทางที่แนะนำ)

บอก agent ว่า "เพิ่มคำว่า ..." แล้วมันจะรัน `python collector/add_keyword.py --interactive` ให้ ซึ่งจะถามสองอย่าง:

```
คำที่อยากเพิ่ม (พิมพ์ให้ตรงกับที่คนค้นจริง): จำนำทะเบียนรถ

คำนี้อยู่กลุ่มไหน
  1) สถาบันการเงินในระบบ - แสวงหาสินเชื่อ    เช่น สินเชื่อธนาคาร, บัตรกดเงินสด, กยศ
  2) สถาบันการเงินในระบบ - สัญญาณเปราะบาง    เช่น พักหนี้, ประนอมหนี้ธนาคาร, หนี้เสีย NPL
  3) ช่องทางดิจิทัลใหม่ - แสวงหาสินเชื่อ      เช่น กู้เงินด่วนออนไลน์, สินเชื่อออนไลน์
  4) ช่องทางดิจิทัลใหม่ - สัญญาณเปราะบาง      เช่น หนี้แอปเงินกู้, โดนหลอกกู้เงินออนไลน์
  5) นอกระบบดั้งเดิม - แสวงหาสินเชื่อ         เช่น จำนำทอง, ขายฝากที่ดิน, เงินกู้นอกระบบ
  6) นอกระบบดั้งเดิม - สัญญาณเปราะบาง         เช่น หนี้นอกระบบ, ดอกเบี้ยโหด, ถูกยึดทรัพย์

เลือก 1-6: 5
```

จากนั้นมันเดินให้ครบทั้งลูปเหมือนรอบเดือน สร้างคิวเฉพาะ ID นั้น หยุดที่ Chrome ให้ Import และ Start พอเก็บครบ 6 พื้นที่ก็กลับมาพิมพ์ `FINISH` แล้วมันจะตรวจด่าน ตั้ง Tier และเอาเข้าคลังให้เอง

ถ้าออกมาเป็น T2 มันจะแสดงเฉพาะครอบครัวที่กลุ่มและทิศทางตรงกัน ให้เลือกเป็นตัวเลขเหมือนกัน

### ทางพิมพ์คำสั่งเอง

ถ้าถนัดพิมพ์มากกว่า หรืออยากเพิ่มหลายคำรวดเดียว

```bash
.venv/bin/python collector/add_keyword.py "จำนำทะเบียนรถ" --prefix TP
```

มันจะเช็คให้ก่อนว่าคำซ้ำกับที่มีอยู่ไหม และเคยลองแล้วตายที่ขั้นไหนไหม (ถ้าเคยตายจะไม่ยอมเพิ่มจนกว่าจะใส่ `--force`)
ทุกครั้งที่เขียน มันสำรอง `keywords.csv` ไว้ที่ `.tools/keywords-backup/` ก่อนเสมอ

เก็บข้อมูลเสร็จแล้วสั่งปิดงาน มันจะอ่านผลด่านคัดกรองแล้วตั้ง Tier ให้ถูกเอง

```bash
.venv/bin/python collector/add_keyword.py --finalize TP004
```

- ออกมา **T1** → ตั้งให้เสร็จ ไปต่อที่ `monthly-finish` ได้เลย
- ออกมา **T2** → มันจะแสดงรายชื่อครอบครัวที่ Segment/Factor ตรงกัน ให้เลือกแล้วรันซ้ำด้วย `--family <ID>`
- **ไม่ผ่าน** → ถอนด้วย `--remove TP004` ซึ่งจะลบแถวให้และบอกว่ามีไฟล์ไหนค้างใน `incoming/` ต้องลบบ้าง

`--remove` จะ**ปฏิเสธถ้าคำนั้นเข้าคลังไปแล้ว** เพราะการถอนตอนนั้นต้องลบไฟล์กับแก้ catalog ด้วย ไม่ใช่งานที่ควรทำแบบอัตโนมัติ

### ตารางรหัสกลุ่ม

ตัวอักษรแรกคือ Segment (ช่องทางสินเชื่อ) ตัวที่สองคือ Factor (ทิศทาง)

| prefix | Segment | Factor | ตัวอย่าง |
|---|---|---|---|
| `FP` | Formal | Pull | สินเชื่อส่วนบุคคล |
| `FU` | Formal | Push | พักหนี้ |
| `NP` | Informal-New | Pull | กู้เงินด่วนออนไลน์ |
| `NU` | Informal-New | Push | หนี้แอปเงินกู้ |
| `TP` | Informal-Traditional | Pull | จำนำทอง |
| `TU` | Informal-Traditional | Push | หนี้นอกระบบ |

**นี่คือสิ่งเดียวที่ต้องใช้คนตัดสิน** คำนี้เป็นสถาบันการเงินในระบบ นอกระบบแบบใหม่ หรือนอกระบบดั้งเดิม และมันแสดงถึงการแสวงหาสินเชื่อหรือสัญญาณเปราะบาง
เลือกผิดแก้ทีหลังแพง เพราะไฟล์ที่โหลดมาผูกกับ ID ต้องเก็บใหม่ทั้ง 6 ไฟล์

เลขที่ใช้ไปแล้วในแต่ละ prefix ดูได้จาก `keywords.csv` โดยตรง (ตอนสร้าง repo นี้ทุก prefix ใช้ถึง 003 คือ FP001–003, FU001–003, TP001–003, TU001–003, NP001–003, NU001–003) `add_keyword.py` หาเลขว่างถัดไปให้เองเสมอ ไม่ต้องนับมือ

### ครอบครัวที่มีอยู่

ใช้ตอนที่ด่านตรวจบอกว่าเป็น T2 — **repo นี้ยังไม่มีครอบครัวไหนเลย** เพราะยังไม่มีคำใดผ่านด่านคัดกรองจริง ตารางนี้จะเริ่มมีแถวเมื่อคำแรกออกมาเป็น T2 (ให้เพิ่มแถวในตารางนี้ตอนนั้น)

ถ้าคำใหม่เป็น T2 แต่ไม่เข้าครอบครัวไหนเลย **เพิ่มคำเดียวไม่ได้** ครอบครัวใหม่ต้องมีอย่างน้อย 2 คำ ต้องหาคำที่สองที่ความหมายใกล้กันมาเพิ่มพร้อมกัน

### ถ้าอยากกรอกเองด้วยมือ ให้ตัดสินว่าเป็นคำเดี่ยวหรือเข้าครอบครัว

กฎนี้บังคับโดยระบบ ผิดแล้วมันไม่ยอมทำงานเลย ไม่ใช่แค่เตือน

**คำเดี่ยว (T1)** `Case_ID` ต้องเท่ากับ `Keyword_ID` เป๊ะ มีสมาชิกตัวเดียว ช่อง `Family_ID` กับ `Family_Name_TH` ต้องว่าง

```
FP004,คำใหม่ของคุณ,T1,Formal,Pull,FP004,keyword,,,active_current_official
```

**เข้าครอบครัว (T2)** `Family_ID` ต้องเท่ากับ `Case_ID` และ **ครอบครัวหนึ่งต้องมีอย่างน้อย 2 คำ** สร้างครอบครัวใหม่ด้วยคำเดียวไม่ได้
ทุกสมาชิกในครอบครัวเดียวกันต้องมี `Tier`, `Segment`, `Factor`, `Family_Name_TH` ตรงกันหมด

```
FP004,คำใหม่ของคุณ,T2,Formal,Pull,FPF01,family_member,FPF01,ชื่อครอบครัว,active_current_official
```

**เลข ID ต้องไม่ซ้ำทั้งใน `keywords.csv` และ `keywords_tried.csv`** ส่วนครอบครัวใช้รูป `<คำนำหน้า>F<เลขสองหลัก>` เช่น `FPF01`

### แล้วเก็บเฉพาะคำนั้น

```bash
./scripts/toolkit.sh monthly-run --ids FP004
```

ไม่ใส่ `--geo` แปลว่าเก็บครบทั้ง 6 พื้นที่

### สองเรื่องที่ต้องรู้ก่อนลงมือ

**เรื่องที่ 1 พอเพิ่มแถวปุ๊บ ด่านตรวจจะแดงทันที**
ระบบคิดว่าต้องมีข้อมูลครบทุกคำคูณ 6 พื้นที่ เพิ่มคำ 1 คำ แปลว่ามี 6 ช่องที่ยังว่างทันที และมันจะไม่ยอมให้ผ่านจนกว่าจะเก็บครบ
**แปลว่าห้ามเพิ่มแถวแล้วทิ้งไว้** ต้องเพิ่มแล้วเก็บให้จบในคราวเดียว ไม่งั้นรอบเดือนถัดไปจะพังโดยที่ไม่ได้ทำอะไรผิด

**เรื่องที่ 2 เพิ่มคำเข้าครอบครัวเดิม ทำให้เส้นของครอบครัวนั้นเปลี่ยนย้อนหลังทั้งเส้น**
เพราะเส้นครอบครัวคือค่าเฉลี่ยของสมาชิก พอสมาชิกเปลี่ยน ค่าเฉลี่ยก็เปลี่ยนทุกเดือนย้อนไปถึงปี 2004 **ไม่ใช่แค่เดือนใหม่**
ผลคือกราฟที่เคยส่งไปในรายงานเก่าจะไม่ตรงกับตัวเลขชุดใหม่อีกต่อไป ถ้าจะทำต้องบอกคนใช้ข้อมูลล่วงหน้า
เพิ่มเป็นคำเดี่ยว T1 ไม่มีปัญหานี้ เพราะไม่ไปยุ่งกับเส้นของใคร

### ถอนคำออก

ถ้ายังไม่ได้เอาเข้าคลัง (ตรวจด้วย `--incoming` แล้วไม่ผ่าน) ทำแค่ 2 อย่าง

1. ลบแถวออกจาก `keywords.csv`
2. ลบไฟล์ของคำนั้นออกจาก `incoming/`

**ถ้าเอาเข้าคลังไปแล้ว ต้องทำ 3 ที่ให้ครบ** ไม่งั้น `audit.py --strict` จะพัง เพราะมันเทียบไฟล์ในคลังกับรายชื่อคำ ไฟล์ที่ไม่มีเจ้าของถือเป็น error

1. ลบแถวออกจาก `keywords.csv`
2. ลบไฟล์ `data/series/<ID>__<GEO>.csv` ทั้ง 6 ไฟล์
3. ลบ 6 รายการของคำนั้นออกจาก `data/catalog.json`

ข้อ 3 ต้องแก้ JSON ด้วยมือ **ถ้าไม่มั่นใจให้ถาม AI agent ทำให้** แล้วรัน `audit.py --strict` ยืนยันว่ากลับมาเขียวก่อนทำอย่างอื่นต่อ
นี่คือเหตุผลที่ควรตรวจด้วย `--incoming` ก่อนเสมอ จะได้ไม่ต้องมาถึงขั้นนี้

### จังหวะที่ควรทำ

**ระบบบังคับให้อยู่แล้ว** ถ้ายังมีไฟล์ค้างใน `incoming/` แปลว่ารอบเก็บก่อนหน้ายังไม่จบ ทั้งการเพิ่มคำใหม่และ `monthly-prepare` จะปฏิเสธที่จะสร้างคิวใหม่ และจะไม่แตะ `keywords.csv` เลย เพราะคิวใหม่จะไปทับคิวที่กำลังวิ่งในหน้าต่างคุมงาน

ทำ **ทันทีหลังรอบเดือนผ่านเรียบร้อย** ตอนนั้นทุกช่องอยู่ที่เดือนล่าสุดพร้อมกันหมด เก็บคำใหม่เสร็จด่านจะผ่านทันที
อย่าทำคาบเกี่ยวกับรอบเดือน และอย่าทำก่อนวันที่ต้องส่งรายงาน

---

## 4. ตอบว่าตัวเลขไหนพูดได้

จะมีคนมาถาม และคำตอบที่ผิดจะไปโผล่ในรายงาน สี่ข้อนี้ตอบได้เกือบทุกคำถาม

1. **ค่าเป็นดัชนี 0 ถึง 100 ไม่ใช่จำนวนคน** และในชุดนี้แต่ละคำถูกดึงแยกกัน จึงปรับฐานคนละฐาน **เทียบขนาดข้ามคำไม่ได้** ถ้าอยากเทียบข้ามคำจริงต้องดึงใหม่โดยใส่คำพร้อมกันในกราฟเดียว
2. **เส้นอีสานเป็นค่าที่คำนวณขึ้น** ต้องดู `N/5` ทุกครั้ง ไม่ครบ 5 จังหวัดห้ามเรียกว่ารวม 5 จังหวัด
3. **no-data กับ WEAK ไม่ใช่หลักฐานว่าครัวเรือนไม่มีความเปราะบางทางการเงิน** no-data คือต่ำกว่าเกณฑ์ที่ Google ยอมรายงาน ส่วน WEAK คือเกณฑ์ของเครื่องมือนี้เอง คือมีเดือนที่ค่าเป็นศูนย์เกิน 25% ของช่วง 2014-01 ถึงเดือนล่าสุด
4. **ข้อมูลระดับจังหวัดเริ่มปี 2014 เท่านั้น** ระดับประเทศเริ่ม 2004

**เรื่องการปรับฤดูกาล** เปิด `derived/sa_pipeline_v3/quality_flags.csv` ดูได้ เคสที่ `Quality_Status = REVIEW` ยังใช้ได้ แต่แปลว่าค่าสถิติคุณภาพของ X-13 ไม่ผ่านเกณฑ์มาตรฐาน ซึ่งปกติสำหรับข้อมูลค้นหา เพราะเกณฑ์นั้นออกแบบมาสำหรับข้อมูลผลผลิตและการจ้างงาน
**เวลาอ้างตัวเลขให้เขียนกำกับว่า "ปรับฤดูกาลด้วย X-13 โดยค่าสถิติคุณภาพยังไม่ผ่านเกณฑ์มาตรฐาน"** อย่าเขียนลอยๆ ว่าปรับฤดูกาลแล้ว

**เรื่องจุดสุดท้ายของเส้น** คอลัมน์ `MA3_Endpoint_Provisional` เป็น `TRUE` แปลว่าจุดล่าสุดของเส้น `MA3_Centered` ยังไม่นิ่ง ต้องรอข้อมูลเดือนถัดไป **ห้ามพาดหัวด้วยจุดนั้น**

---

## 5. ตั้งเครื่องใหม่

เครื่องเปล่าที่ยังไม่มีอะไรเลย ให้ทำ 3 ช่วงนี้ตามลำดับ ห้ามสลับ

### ช่วงที่ 1 ลงของ 4 อย่าง

เปิด Terminal แล้ววางทีละบรรทัด (ต้องมี [Homebrew](https://brew.sh) ก่อน)

```bash
brew install python@3.13 git gh
brew install --cask google-chrome
```

### ช่วงที่ 2 ตั้งตัวตนแล้วโคลน

```bash
git config --global user.name "<ชื่อจริงของคนที่ดูแล>"
git config --global user.email "<อีเมลจริงของคนที่ดูแล>"
gh auth login
gh auth setup-git
git clone <URL ของ repo นี้บน GitHub>
cd isan-household-fragility-toolkit
./scripts/toolkit.sh setup
```

สองบรรทัดแรกใส่ชื่อกับอีเมลจริงของคนที่ดูแล ไม่ใช่ของตัวอย่าง เพราะมันจะไปโผล่ในประวัติการเผยแพร่ว่าใครเป็นคนอัพเดทเดือนไหน

`gh auth login` จะถาม 5 ข้อ ตอบตามนี้

| มันถาม | ตอบ |
|---|---|
| What account do you want to log into? | `GitHub.com` |
| What is your preferred protocol for Git operations? | `HTTPS` |
| Authenticate Git with your GitHub credentials? | `Y` |
| How would you like to authenticate GitHub CLI? | `Login with a web browser` |
| First copy your one-time code: `ABCD-1234` | จำโค้ดไว้ กด Enter แล้วเบราว์เซอร์จะเปิดเอง เอาโค้ดไปวางแล้วกด Authorize |

`gh auth login` ต้องมาก่อน `git clone` เสมอ

**คำสั่ง `setup` ตรวจของ 4 อย่างในช่วงที่ 1 ให้ ไม่ได้ลงให้** ถ้าขาดอะไรมันจะฟ้องแล้วหยุด

| ต้องมีก่อน | ถ้าไม่มีจะเจอ |
|---|---|
| Python 3.11 ถึง 3.13 (3.14 ใช้ไม่ได้) | `Python 3.11-3.13 not found` |
| Git และตั้งชื่อกับอีเมลผู้ใช้แล้ว | `Git author missing` |
| Google Chrome | `Google Chrome not found` |
| GitHub CLI ที่ล็อกอินแล้ว | `GitHub CLI is not signed in` |
| X-13 binary | ติดตั้งเองครั้งแรก — ดู README.md หัวข้อ "แพลตฟอร์มและ X-13" (ไม่มี auto-installer บน Mac) |

> **ข้อควรระวัง** ถ้ายังไม่ได้ล็อกอิน GitHub ระบบจะ**ข้ามการตรวจสุขภาพทั้งชุดไปเงียบๆ** แล้วเราจะไม่รู้เลยว่าเครื่องนั้นใช้งานได้จริงไหม เพราะฉะนั้นต้องล็อกอินให้เรียบร้อยก่อนรัน `setup`

### ช่วงที่ 3 ตั้ง Chrome ด้วยมือ

ช่วงนี้พิมพ์คำสั่งแทนไม่ได้ และเป็นส่วนที่พังบ่อยที่สุด **ตอน `setup` จบมันพิมพ์ path ที่ต้องใช้ให้ครบแล้ว ลอกจากหน้าจอไปเลย ไม่ต้องพิมพ์เอง**

```
MACHINE READY
1. Load unpacked extension from: /Users/<ชื่อ>/isan-household-fragility-toolkit/extension
2. Allow trends.google.co.th and set Chrome Downloads to: /Users/<ชื่อ>/isan-household-fragility-toolkit/incoming
3. Turn off 'Ask where to save each file'
```

1. ใช้ Chrome ที่ลงชื่อเข้าใช้ Google แล้ว
2. ไป `chrome://extensions` เปิด Developer mode มุมขวาบน กด Load unpacked แล้วเลือกโฟลเดอร์ตามข้อ 1 บนหน้าจอ
3. ไป `chrome://settings/downloads` กด Change แล้ววาง path ตามข้อ 2 บนหน้าจอ
4. ปิดสวิตช์ **Ask where to save each file**

### แล้วพิสูจน์ว่าเครื่องใช้ได้จริง

**`setup` ไม่รู้ว่าตั้ง Chrome ถูกหรือยัง** มันเช็คแค่ว่ามี Chrome อยู่ในเครื่อง การทดสอบจริงข้อเดียวที่มีคือรันคิวคำเดียว

```bash
./scripts/toolkit.sh monthly-run --ids FP001 --geo TH
```

เห็น `MONTHLY CHECKS PASSED` เมื่อไหร่ ถือว่าเครื่องนั้นพร้อมจริง ก่อนหน้านั้นยังไม่นับ

---

## 6. เมื่อเจอสิ่งที่ไม่รู้จัก

ก่อนอื่น **เก็บหลักฐานไว้ก่อน** กด Copy Debug ในหน้าต่างคุมงาน และเก็บข้อความใน terminal ไว้ทั้งหมด

แล้วสั่งตรวจสุขภาพดูว่าระบบมองเห็นอะไร

```bash
.venv/bin/python collector/audit.py --strict --require-latest
```

คำสั่งนี้อ่านอย่างเดียว ไม่แก้อะไร รันซ้ำได้ไม่มีผลเสีย

**ถ้าอาการคือตัวเก็บทำงานไม่ได้เลย เช่นกดแล้วไม่มีอะไรเกิดขึ้น หรือโหลดไฟล์มาแล้วอ่านไม่ออก** มีโอกาสสูงว่า Google เปลี่ยนหน้าเว็บ (เคยเกิดกับ engineering ต้นทางของ toolkit นี้มาแล้วหลายครั้ง) งานนี้ต้องแก้โค้ด

**ทางที่ทำได้คือเปิด AI agent ที่โฟลเดอร์นี้** แล้วเล่าอาการพร้อมแปะหลักฐานที่เก็บไว้ให้มัน มันจะอ่าน `SKILL.md` เองแล้วรู้กติกาทั้งหมด
ถามให้มันอธิบายก่อนว่าจะแก้อะไรและทำไม อย่าให้มันแก้แล้วเราไม่รู้ว่าแก้อะไร และให้มันรายงานผลเป็นตัวเลขนับได้เสมอ เช่น เก็บได้กี่ไฟล์ กี่ซีรีส์ ถึงเดือนไหน

**ถ้าแก้ไม่ได้จริงๆ ข้อมูลที่เก็บมาแล้วยังอยู่ครบและยังเปิดใน Tableau ได้ตามปกติ** สิ่งที่หายไปคือความสามารถในการเก็บเดือนใหม่เท่านั้น ไม่ใช่ทั้งหมด

---

## กติกาเหล็ก 8 ข้อ

ห้ามละเมิดไม่ว่าจะรีบแค่ไหน และ**ห้ามปล่อยให้ AI agent ละเมิดด้วย**

1. **ห้ามเอาข้อมูลคนละช่วงเวลามาต่อกันเป็นเส้นเดียว** การอัพเดทที่ถูกคือดึงใหม่ทั้งเส้นแล้วแทนที่ทั้งเส้น
2. **ดึงยาวสุดเสมอ** ตั้งแต่ 2004-01-01 ถึงวันนี้ ข้อมูลระดับจังหวัดก่อนปี 2014 ระบบตัดทิ้งให้เอง
3. **ห้ามแก้ `data/` และ `data.js` ด้วยมือ** ต้องผ่านตัวเก็บและขั้นตอนนำเข้าเท่านั้น
4. **ตัวเก็บทุกตัวต้องใช้ช่วงเวลามาตรฐานเดียวกัน** ระบบจะปฏิเสธค่าอื่นก่อนเริ่มเก็บ ห้ามหาทางเลี่ยง
5. **ห้ามใช้ตัวเลือกที่ตัดข้อมูลเก่าทิ้ง** มันถูกปิดไว้โดยตั้งใจ
6. **เพิ่มคำใหม่ต้องเช็ค `reference/keywords_tried.csv` ก่อนทุกครั้ง**
7. **ตัวเลขคือดัชนี ไม่ใช่จำนวนการค้นจริง** ห้ามสรุปเป็นจำนวนคน และห้ามเทียบขนาดข้ามคำ
8. **รายงานผลเป็นตัวเลขนับได้เสมอ** กี่ไฟล์ กี่ซีรีส์ กี่เดือน ถึงเดือนไหน ถ้าผลไม่ตรงที่คาด ให้หยุดแล้วหาสาเหตุ อย่าเดินต่อเงียบๆ

มีอีกสองตัวเลือกในระบบชื่อ pytrends กับ browser runner **ทั้งคู่เป็นเครื่องมือวินิจฉัยเท่านั้น ผลจากสองตัวนี้ห้ามนำเข้าคลังและห้ามเผยแพร่** ไม่ว่า AI agent จะเสนอมาแบบไหนก็ตาม
